# cd.yml (Versi Awal / Sebelumnya)
name: CD - Deploy to Google Cloud Run

on:
  push:
    branches: [main, staging] # Dipicu oleh push, TIDAK menunggu CI

jobs:
  deploy:
    name: Deploy to Cloud Run
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Google Cloud SDK
        uses: google-github-actions/setup-gcloud@v1

      - name: Build and push Docker image
        # CATATAN: Ini membangun ulang image, tidak menggunakan yang dari CI
        run: |
          gcloud builds submit --tag gcr.io/${{ secrets.GCP_PROJECT_ID }}/expense-app

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy expense-app \
            --image gcr.io/${{ secrets.GCP_PROJECT_ID }}/expense-app \
            --platform managed \
            --region ${{ secrets.GCP_REGION }} \
            --allow-unauthenticated